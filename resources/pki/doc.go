package pki

/*
Files for a test Kubernetes pki.
IMPORTANT! this is not used in production and it must never be.

If it is required to recreate, you can copy ca and apiserver crt and key from the /etc/kubernetes/pki
folder of a kind cluster; admin crt and key are inline in /etc/kubernetes/admin.conf file.
*/
